/*
测试函数的默认实参
*
*/

#include<iostream>
#include<string>

using namespace std;

typedef string::size_type sz;
void screen(sz ht=24, sz wid=80, char = ' '){
	for(int i = 0 ; i != ht; ++i)
		for(int j = 0; j!=wid	
}
int main(){

	return 0;
}
